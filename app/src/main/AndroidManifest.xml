<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".MiooDaoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.MiooDao">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing"
            android:theme="@style/Theme.MiooDao"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep links: open thread URLs in-app -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <!-- Supported Hosts -->
                <data android:host="www.nmbxd.com" />
                <data android:host="nmbxd.com" />
                <data android:host="www.nmbxd1.com" />
                <data android:host="nmbxd1.com" />
                <data android:host="www.adnmb.com" />
                <data android:host="adnmb.com" />
                <data android:host="www.adnmb1.com" />
                <data android:host="adnmb1.com" />
                <data android:host="www.adnmb2.com" />
                <data android:host="adnmb2.com" />
                <data android:host="www.adnmb3.com" />
                <data android:host="adnmb3.com" />
                <data android:host="www.adnmb4.com" />
                <data android:host="adnmb4.com" />
                <data android:host="www.adnmb5.com" />
                <data android:host="adnmb5.com" />
                <data android:host="adnmb.co" />
                <data android:host="www.adnmb.co" />
                <data android:host="nmb.fastmirror.org" />
                <!-- Supported Path Formats -->
                <data android:pathPrefix="/t/" />
                <data android:pathPrefix="/thread/" />
                <data android:pathPrefix="/Forum/thread/id/" />
            </intent-filter>

            <!-- Share text containing thread links / No.xxx -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.screens.settings.CustomCaptureActivity"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Disable default WorkManager init; Application provides Hilt WorkerFactory -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
            
    </application>
</manifest>
